import { Box, Heading, Text } from '@chakra-ui/react'

function CommittedPlanPage() {
  return (
    <Box p={8}>
      <Heading size="lg" mb={4}>
        Committed Plan
      </Heading>
      <Text color="gray.600">
        Committed Plan goes here.
      </Text>
    </Box>
  )
}

export default CommittedPlanPage
